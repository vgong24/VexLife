# Patient-0 P8/P9 — Familiarity, relationship boundary and bounded live work

Continuity: `[VXG RealForever]`  
Allocation: `github.issue.vexlife.36`  
Product composition owner: `VEXLIFE`  
Portable contract owner: `SDK_OPERATIONS`

This seam composes formative Patient-0 P8/P9 evidence without making VexLife the owner of durable Memory, consent, standing relationship authority, or graduation.

## P8 ladder

```text
EPHEMERAL
→ FAMILIARITY
→ UNDERSTANDING
→ DURABLE_MEMORY only through explicit source-bound VEX_MEMORY acceptance
```

Hard distinctions:

```text
noticed != will remember
familiarity != standing relationship authority
recognition != authentication != authorization
```

A P8 relationship-boundary receipt includes a meaningful user-facing notification. It may say that context has been noticed or become familiar while also stating that no durable-memory promise or standing authority is implied.

## P9 bounded work

```text
bounded task
→ exact user scope + allowed source refs
→ source-addressable self-witness
→ lesson candidate
→ repeat / transfer evidence where required
→ reusable-capability candidate
```

A reusable-capability candidate is still a candidate. This module never accepts graduation, never grants authority, and never promotes a lesson into durable Memory.

## Owner boundary

```text
VEXLIFE
  lived journey and bounded-work composition

VEX_MEMORY
  Memory, Score, provenance and durable promotion

VEX_SAFETY_SECURITY
  consent, private boundary and standing relationship authority

SDK_OPERATIONS
  portable familiarity / graduation contracts

MAIN_VEX
  developmental / graduation meaning

VEX_INTERFACE
  surface / notification / recovery UX
```

The shared SDK contract consumed here is `contract.vex-memory.familiarity-to-durable-memory-promotion.v0`. The portable Patient-0 foundation also preserves `capability learned != authority inherited` and denies transfer of Patient-0 private autobiography, Victor/Patient-0 relationship identity, Home contents, credentials, private third-party material and standing authority.

## Real-lived admission

Synthetic tests may prove contract shape, but cannot satisfy P9. A real-lived witness must bind one explicitly bounded task, the exact allowed and observed source refs, source-addressable before/after conversation heads, and a held-effect ledger. Raw private content is not embedded in the witness.

Before a real Patient-0 turn, the host must freshly re-ground the current Home identity/head and provider/runtime state. Historical P4 runtime evidence is not standing authority or currentness.

## Held effects

The composition fails closed if any task attempts:

```text
durable Memory promotion
standing relationship authority grant
personal-data scope widening
unbounded file/repository access
training or weight mutation
automatic Dream/Rhythm promotion
publication
P10 graduation
P11 release
```

P8/P9 output is formative evidence only and returns to the post-P4 convergence map for independent acceptance.
