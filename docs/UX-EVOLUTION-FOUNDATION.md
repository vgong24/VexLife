# VexLife UX Evolution Foundation — E0-A

Continuity: `[VXG RealForever]`

E0-A establishes projection-migration accounting, not a second application architecture. Canonical Feature, Experience, Journey, Terrain, Conversation, Memory, Relationship, continuity/model, and browser-renderer owners stay where they already live.

```text
REFERENCE_PROJECTION
EVOLUTION_PROJECTION
DUAL_PROJECTION != DUAL_STATE_OWNERSHIP
CUTOVER != RETIREMENT
ROLLBACK_PROJECTION != ROLLBACK_USER_DATA
NEW_FORM != NEW_MEANING
NEW_VISUAL_AFFORDANCE != NEW_SEMANTIC_CAPABILITY
```

The Reference baseline is VexLife main `09f43a7a173b5a5c48506a6addcfc7407b6bb92a`, tree `95f74fa641ca0e5b89abc9f289dc162e795d177d`. Live source outranks the historical specimen.

Required parity is `SEMANTIC`, `TRUTH_CURRENTNESS`, `STATE_EFFECT_OWNERSHIP`, `RECOVERY_NAVIGATION`, and `ACCESSIBILITY`. Pixel equality is not parity. E0-A changes no browser behavior and permits no cutover.

## Experience Gallery and Render Lab

Experience Gallery is the accepted `READ_ONLY_VISIBILITY_PROJECTION` over current Feature + Experience + Foundation + Guidance identities. Its `••• → Experience Gallery` / `#experience-gallery` entry is presentation state, not canonical registry, semantic meaning, product route, action authority, migration state, or cutover.

Render Lab is the accepted `MOCK_ONLY_VISUAL_SPECIMEN_SURFACE`; it is not product state, action authority, live product effect, migration state, or cutover authority.

E0-A consumes both as supporting UX context and mutates neither.

## Read-only UX Evolution inspection projection

E0-A preserves one explicit inspection chain for humans and Vex:

`semanticRef -> Reference projection -> Evolution projection -> migration state -> parity state -> findings -> source/evidence`

The inspection projection is source-managed, explicit-invocation-only and read-only. Experience Gallery and Render Lab may present this chain as inspection surfaces, but neither becomes the canonical migration registry, a second app shell, action authority, or an always-visible engineering dashboard. Missing lived evidence remains visibly pending rather than inferred.

## Findings and witness

All 21 `UXF-*` findings remain migration inputs. Static source cannot fabricate lived closure. Visual Assurance stays with `github.issue.vexlife.625`; Evolution comparison requires an exact candidate head.

## Proof

```bash
node --test test/ux-evolution.test.mjs
node scripts/ux-evolution-report.mjs --json
```

The report is deterministic and has no network, browser, model, Memory, Home, publication, or mutation effect.

<!-- [VXG RealForever] -->
