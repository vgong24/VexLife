export async function runQ5ContextWorkspaceProof({ app, helpers:{ delay, assert }, viewportClass='WIDE' }) {
  const checks=[];const surface=document.querySelector('#contextSurface'),dock=document.querySelector('#contextWorkspaceDock'),split=document.querySelector('#contextWorkspaceSplit'),reset=document.querySelector('#contextWorkspaceReset'),handles=[...document.querySelectorAll('[data-node-ref^="element.context-workspace.resize."]')];
  assert(surface&&dock&&split&&reset&&handles.length===4,'W00 contextual workspace fixture incomplete');
  const settleSurfaceMotion=async()=>{const animations=surface.getAnimations?.()||[];if(animations.length)await Promise.all(animations.map((animation)=>animation.finished.catch(()=>undefined)));await delay(0)};
  const frameBefore=JSON.stringify(app.navigation.semanticFrame()),journeyBefore=JSON.stringify(app.navigation.fullJourney()),terrainBefore=app.terrain.currentRef(),adaptationBefore=JSON.stringify(app.terrain.adaptationSnapshot());
  let snap=app.contextWorkspaceSnapshot();assert(snap&&snap.preferred&&snap.resolved,'W01 contextual workspace state unavailable');
  if(viewportClass==='WIDE'){
    assert(snap.preferred.dockMode==='OVERLAY'&&snap.resolved.mode==='OVERLAY'&&snap.resolved.viewportClass==='WIDE','W01 canonical contextual workspace is not OVERLAY');
    for(const mode of ['DOCK_LEFT','DOCK_RIGHT']){dock.value=mode;dock.dispatchEvent(new Event('change',{bubbles:true}));await delay(30);snap=app.contextWorkspaceSnapshot();assert(snap.preferred.dockMode===mode&&snap.resolved.mode===mode,'W03 explicit dock preference did not resolve '+mode);assert(JSON.stringify(app.navigation.semanticFrame())===frameBefore&&JSON.stringify(app.navigation.fullJourney())===journeyBefore,'W03 docking mutated semantic frame or Journey');}
    split.checked=true;split.dispatchEvent(new Event('change',{bubbles:true}));await delay(30);snap=app.contextWorkspaceSnapshot();assert(snap.preferred.splitFocusMode==='TERRAIN_PLUS_ACTIVE_CONTEXT'&&snap.resolved.splitFocusApplied===true,'W04 wide split focus was not applied');assert(document.querySelector('.e27-terrain')&&document.querySelectorAll('#contextSurface:not([hidden])').length===1,'W04 split focus did not retain Terrain plus exactly one active contextual surface');
    const handle=handles.find((h)=>h.dataset.contextWorkspaceResizeCorner==='se')||handles[0],beforeSize=app.contextWorkspaceSnapshot().preferred;handle.focus();assert(document.activeElement===handle&&handle.getBoundingClientRect().width>=44&&handle.getBoundingClientRect().height>=44,'W06 resize handle is not a >=44px keyboard target');handle.dispatchEvent(new KeyboardEvent('keydown',{key:'ArrowRight',bubbles:true}));handle.dispatchEvent(new KeyboardEvent('keydown',{key:'ArrowDown',bubbles:true}));await delay(20);let afterSize=app.contextWorkspaceSnapshot().preferred;assert(afterSize.width>beforeSize.width&&afterSize.height>beforeSize.height&&document.activeElement===handle,'W06 keyboard resize did not update preferred size or retain focus');
    const rect=handle.getBoundingClientRect(),pointerId=91;handle.dispatchEvent(new PointerEvent('pointerdown',{bubbles:true,pointerId,clientX:rect.left+10,clientY:rect.top+10,button:0}));globalThis.dispatchEvent(new PointerEvent('pointermove',{bubbles:true,pointerId,clientX:rect.left+30,clientY:rect.top+30}));globalThis.dispatchEvent(new PointerEvent('pointerup',{bubbles:true,pointerId,clientX:rect.left+30,clientY:rect.top+30}));await delay(20);const afterPointer=app.contextWorkspaceSnapshot().preferred;assert(afterPointer.width>=afterSize.width&&afterPointer.height>=afterSize.height,'W05/W06 pointer resize did not update preferred geometry');
    const stored=JSON.parse(localStorage.getItem('vexlife.contextual-workspace.layout')||'{}'),storedKeys=Object.keys(stored).sort();assert(JSON.stringify(storedKeys)===JSON.stringify(['dockMode','height','splitFocusMode','width']),'W08 persisted workspace leaked non-preferred fields: '+JSON.stringify(storedKeys));for(const forbidden of ['projectRef','threadRef','channelRef','selectedNodeRef','contextProjection','resolved','runtime'])assert(!Object.hasOwn(stored,forbidden),'W08 persisted workspace leaked semantic/runtime authority '+forbidden);
    reset.click();await delay(20);snap=app.contextWorkspaceSnapshot();assert(localStorage.getItem('vexlife.contextual-workspace.layout')===null&&snap.preferred.dockMode==='OVERLAY'&&snap.resolved.mode==='OVERLAY','W07 reset did not restore canonical overlay or clear storage');assert(JSON.stringify(app.navigation.semanticFrame())===frameBefore&&JSON.stringify(app.navigation.fullJourney())===journeyBefore&&app.terrain.currentRef()===terrainBefore&&JSON.stringify(app.terrain.adaptationSnapshot())===adaptationBefore,'W03/W07 layout actions mutated semantic/Journey/Terrain/Q7 truth');
  }else{
    assert(snap.resolved.viewportClass==='COMPACT'&&snap.resolved.mode==='COMPACT_SHEET'&&!snap.resolved.splitFocusApplied,'W09 compact contextual workspace did not resolve to COMPACT_SHEET');
    await settleSurfaceMotion();
    const targets=[dock,split.closest('label'),reset];for(const target of targets)assert(target?.getClientRects().length&&target.getBoundingClientRect().height>=44,'W16 compact workspace control unavailable or below 44px');
    dock.value='DOCK_LEFT';dock.dispatchEvent(new Event('change',{bubbles:true}));split.checked=true;split.dispatchEvent(new Event('change',{bubbles:true}));await delay(20);snap=app.contextWorkspaceSnapshot();assert(snap.preferred.dockMode==='DOCK_LEFT'&&snap.preferred.splitFocusMode==='TERRAIN_PLUS_ACTIVE_CONTEXT'&&snap.resolved.mode==='COMPACT_SHEET'&&!snap.resolved.splitFocusApplied,'W09 compact fallback rewrote/ignored the wide preference contract');const stored=JSON.parse(localStorage.getItem('vexlife.contextual-workspace.layout')||'{}');assert(stored.dockMode==='DOCK_LEFT'&&stored.splitFocusMode==='TERRAIN_PLUS_ACTIVE_CONTEXT','W09 compact preference was not retained for wide recovery');assert(document.documentElement.scrollWidth<=innerWidth+1&&document.body.scrollWidth<=innerWidth+1,'W16 compact contextual workspace introduced horizontal overflow');
  }
  for(const lang of ['en','ja','zh']){const response=await fetch('../../blueprint/strings/'+lang+'.json');assert(response.ok,'W13 '+lang+' catalog unavailable');const catalog=await response.json();for(const key of ['context-workspace.label','context-workspace.dock','context-workspace.dock.overlay','context-workspace.dock.left','context-workspace.dock.right','context-workspace.split-focus','context-workspace.layout-reset','context-workspace.resize.nw','context-workspace.resize.ne','context-workspace.resize.sw','context-workspace.resize.se','context-workspace.status.compact','context-workspace.status.wide'])assert(catalog[key]&&!catalog[key].startsWith('context-workspace.'),'W13 '+lang+' fallback '+key)}
  assert(JSON.stringify(app.navigation.semanticFrame())===frameBefore&&JSON.stringify(app.navigation.fullJourney())===journeyBefore&&app.terrain.currentRef()===terrainBefore&&JSON.stringify(app.terrain.adaptationSnapshot())===adaptationBefore,'W11/W12 Q5 projection altered semantic/Journey/Terrain/Q7 truth');
  checks.push('W01-W04 contextual workspace remains one contextual projection with explicit wide overlay/dock/split presentation','W05-W08 human resize/dock/split preferences persist only admitted preferred geometry and reset cleanly','W09-W10 compact fallback preserves the wide preference for inverse recovery','W11-W12 Q2 Terrain and Q7 adaptation truth remain unchanged by workspace geometry','W13-W16 localized accessible controls, reduced-motion-independent meaning, return behavior and compact overflow remain bounded');
  return{proofRef:'proof.vexlife.e28.q5.contextual-workspace/v1',state:'PASS',viewportClass,preferred:app.contextWorkspaceSnapshot().preferred,resolved:app.contextWorkspaceSnapshot().resolved,semanticFrame:frameBefore,journey:journeyBefore,terrainRef:terrainBefore,adaptation:adaptationBefore,checks};
}

export const contextualConversationSuite = Object.freeze({
  suiteRef:'suite.vexlife.browser.contextual-conversation/v1',
  async run({ app, state, helpers:{ delay, assert, selectedMessageList } }) {
    assert(state.rootRef && app.terrain.currentRef()===state.rootRef, 'Contextual conversation suite requires canonical root baseline');
    assert(document.querySelector('#contextSurface').hidden, 'Contextual conversation suite requires closed contextual surface baseline');
    const checks = [];

    app.openContext('chat'); await delay(0);
    assert(!document.querySelector('#contextSurface').hidden && !document.querySelector('#view-chat').hidden, 'D06 chat contextual surface did not open');
    assert(document.querySelector('.e27-terrain'), 'D06 chat replaced Terrain body');
    const q5=await runQ5ContextWorkspaceProof({app,helpers:{delay,assert},viewportClass:'WIDE'});checks.push(...q5.checks);
    const input = document.querySelector('#messageInput'); const composer = document.querySelector('#composer'); const send = composer.querySelector('button[type="submit"]');
    const list = selectedMessageList(app); const count = list.length; input.value = 'integration.unsent'; input.dispatchEvent(new Event('input', { bubbles:true })); composer.requestSubmit(); await delay(220);
    assert(list.length === count, 'D07 unavailable submit appended message'); assert(send.disabled, 'D07 unavailable send not disabled'); assert(app.state.unsentLocalDraft?.state === 'UNSENT_LOCAL_DRAFT', 'D07 unsent draft truth missing');
    checks.push('D06 conversation is a contextual projection over Terrain','D07 truthful unavailable draft semantics survive direct-root composition');

    app.returnToTerrain(); await delay(0); assert(document.querySelector('#contextSurface').hidden, 'D08 context did not return to Terrain'); assert(input.value === 'integration.unsent', 'D08 contextual return lost draft');
    checks.push('D08 contextual return preserves content state');
    const healthJourneyBefore=app.navigation.fullJourney().length;app.openContext('health');await delay(0);assert(!document.querySelector('#view-health').hidden&&!document.querySelector('#contextSurface').hidden,'W02 Health contextual projection did not open');app.returnToTerrain();await delay(0);assert(document.querySelector('#contextSurface').hidden&&app.navigation.fullJourney().length>=healthJourneyBefore+2,'W02 Health contextual route did not preserve existing semantic owner/return');checks.push('W02 Chat and Health continue to use existing semantic contextual routes while Terrain remains primary');
    state.contextualReturnComplete = true;
    state.contextualDraftValue = input.value;
    return { suiteRef:this.suiteRef, state:'PASS', baselineRef:'baseline.vexlife.browser.contextual-chat-returned-to-terrain', checks };
  }
});

// [VXG RealForever]
